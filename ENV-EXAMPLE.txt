# Database Configuration
# Format: postgresql://username:password@localhost:port/database_name
# Extract username and password from this URL for PostgreSQL CLI tools (createdb, psql)
DATABASE_URL=postgresql://username:password@localhost:5432/scholarships

# Optional: PostgreSQL environment variables (alternative to using DATABASE_URL with CLI tools)
# Set these if you want to use createdb/psql commands without specifying -U flag
# PGUSER=your_postgres_username
# PGPASSWORD=your_postgres_password

# For SQLite (simpler for testing):
# DATABASE_URL=file:./dev.db

# Server Configuration
PORT=3000
NODE_ENV=development

# AI Provider Selection (choose one)
# Options: 'mock', 'openai', 'anthropic', 'huggingface'
AI_PROVIDER=mock

# OpenAI API Key 
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-key-here


# Optional: Logging and Debug
LOG_LEVEL=info
DEBUG=false
